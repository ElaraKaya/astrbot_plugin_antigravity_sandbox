# 更新日志

## 1.6.2 - 2026-09-25

- 新建任务按各 Key 本地 `in_progress` 做负载均衡：优先交给更空闲的 Key；剩余额度相同则提交到上一把 Key 的下一把，已满的跳过。全部占满时仍先刷新一次，没有空闲再提示目前无空余沙盒分配。续接不换 Key。

## 1.6.1 - 2026-09-25

- 取回查询 GET 的 HTTP 500（`Internal error encountered` / `api_error`）并入超时与 504 的同一兜底：原地重试一次，仍失败则回「任务可能仍在跑」，不再把原始 500 JSON 发给用户。HTTP 200 正文里出现同样字样不会被误判。

## 1.6 - 2026-09-25

- 沙盒文件页拉取改为服务端真实进度：约 500ms 推一次，可取消正在进行的下载，同时最多两路。聊天侧拉取不占用这条并发。
- 每个 Key 的 `in_progress` 任务有上限（默认 1）。新建任务自动换到仍有空闲的 Key；全部占满时先刷新相关短号一次，仍无空闲则提示目前无空余沙盒分配。续接不换 Key，达到上限直接拒绝。
- 续接附件改为 PUT 写入已有沙盒，不再走 interaction sources。无后缀按 md 写入；不写产出类型时默认 md，并返回对应预期网址。写入失败仍继续续接。
- 新增回执截断字符数配置，默认 2000。只有确定会发图片回执时不截断。
- 新增出站代理配置，留空直连。图床 Webhook 不走该代理。
- 新增 `/agls`、`/agget`，以及 LLM 工具 `list_sandbox_task`、`get_sandbox_task`。`/agls` 渲染成表格图片发送，失败时回退制表符文本列表。`/agget` 按完整路径拉取：`/agget <短号>:<路径>`，或只有一个沙盒时 `/agget <路径>`；旧写法 `<短号> <文件名>` 仍兼容。拉取超过 20MB 或 90 秒会中止。
- 旧指令增加短指令：`/ags`、`/agr`、`/agc`、`/agels`、`/agecl`。
- 提交/续接回执把「任务编号」提到首行，并把 `/agls`、`/agget` 的后续用法一并写进回执。用户可见文案里的 `taskid` 标签统一改为「任务编号」，帮助文案同步。
- 取回查询 GET 超时从 15s 压到 7s，超时或命中 504 / `deadline_exceeded` 时原地重试一次，依旧失败才抛「可能是 09 沙盒 bug」的回执。
- `background=true` 时自动附带 `store=true`，让任务在 Google 侧落库，否则后续按 id 取回 / 续接时背面链路可能查不到。

## 1.5.15 - 2026-09-23

- 优化 `_is_image_blank` 异常处理：检测图片空白异常时记录 warning 并默认视为非空白（False），避免异常时误判导致正常图片被静默丢弃。
- 支持沙盒根目录与任意路径文件浏览：
  - 修复 `normalize_environment_file_path` 默认强行填充 `workspace` 的问题，允许空路径正确定位到沙盒根目录 `/`。
  - 打开沙盒时先尝试列出 `workspace`，若返回 404/not_found 则自动改列根目录，避免因无 `workspace` 目录导致无法浏览根目录下的 `*.tar.gz` 等文件产物。
  - 前端路径栏支持显示真实当前路径并提供面包屑导航，支持点击目录进入与返回上级。
  - 根目录列表支持默认折叠系统目录（如 `.agents`、`usr` 等）并提供展开/折叠开关与置底弱化显示，同时完整保留用户可见文件（如 `*.tar.gz`、用户目录等）。
- 修复回执图片 HTML 模板注入截断与标签未闭合：补全 `</textarea>` 闭合标签并在 `</body>` 前安全注入隐藏容器，避免破坏原模板标签属性与 DOM 结构。
- 修复沙盒文件 Web 下载临时文件残留泄漏：改用 `stream_response` 配合异步生成器流式传输，零临时文件落盘，彻底解决 `/tmp` 磁盘空间堆积。
- 修复大模型工具 `submit_sandbox_task` 本地文件路径安全校验：增加 `is_safe_local_file_path` 严格拦截越权读取系统关键目录（如 `/etc`、敏感配置等），防范提示词注入导致的主机文件泄漏。
- 修复沙盒文件 Web 上传无扩展名文件（如 `Dockerfile`、`Makefile`）被误判为目录的问题。
- 修复短号索引裁剪后未立即调用 `_save_short_index()` 持久化的问题。
- 修复模型提示词数组中隐式字符串拼接缺失逗号的问题。
- 同步统一 `README.md` 与 `metadata.yaml` 的版本号。

## 1.5.14 - 2026-09-23

- 修复取回误判：HTTP 200 成功响应体若包含 `deadline_exceeded` 等字样（例如任务产物里贴了插件源码）不再被当成 504/网关失败。

## 1.5.13 - 2026-09-23

- 取回查询 GET 超时从 60s 缩短为 15s（`RETRIEVE_GET_TIMEOUT`），仅影响 interaction 状态查询，不改提交/续接 POST。
- `/agretrieve` 与 `/agcontinue` 自动取回在超时、网络错误、HTTP 504 / `deadline_exceeded` 时返回明确中文回执：取回失败但任务可能仍在沙盒运行，提示可能是 antigravity-preview-09 沙盒取回 bug，并建议稍后重试。
- 避免原先空的 `查询任务网络错误: ` 原始后缀直接抛给用户。

## 1.5.12 - 2026-09-23

- Dashboard 插件页新增「沙盒文件」：浏览环境 workspace、下载/上传文件、删除沙盒（二次确认）。
- `gemini_client` 封装 Environments Files API：`list_environment_files` / `download_environment_file`（含流式）/ `upload_environment_file`（resumable）。
- 注册 Web API：`/{plugin}/sandbox-files/tasks|files|download|upload/<sandbox_id>|delete`；前端走 `AstrBotPluginPage` bridge。
- 增加 `.astrbot-plugin/i18n` 中 `pages.sandbox-files` 文案。
- 修复 Environments Files 列表误传 `pageSize`/`pageToken` 导致 Gemini 400、文件台空白。
- 任务列表默认只读本地短号索引（可选 `include_remote=1`），避免扫全量远端环境。
- 修复 Plugin Page 内联脚本早于 bridge SDK 注入导致「未检测到 bridge」：显式引入 SDK 并等待就绪。
- 传输队列支持终止进行中的下载/上传（取消排队或打断等待；AstrBot bridge 暂不提供 HTTP abort，父页可能仍会完成一次落盘）。


## 1.5.11 - 2026-09-22

- 去除多余上传与图床依赖：回执图片渲染后直接发送本地图片组件，不再二次上传至图床，彻底剥离对 `image_host.enabled` 的依赖，不再拼接和发送「参考链接: <image_url>」。清理无用上传函数与死代码。
- 新增字数阈值配置项 `image_receipt_min_length`（默认 200）：在 `receipt` 配置分组中新增字数阈值控制。开启图片回执且状态为 `completed` 时，仅当回执正文字符数大于或等于该阈值时才渲染为图片发送；短文本直接返回纯文本回执，避免短内容滥用文转图。
- 兼容与兜底保障：配置缺失时平滑回退默认 200 字；阈值设置为 0 或负数时按不限制字数处理（任意长度只要完成即转图）；保留空白图片检测与本地策略兜底机制，渲染失败时安全回退纯文本发送。

## 1.5.10 - 2026-09-22

- 修复回执图片渲染失败导致正文变空白（如只有标题条白屏）：针对部分环境与历史模板中将正文直接内联至 JS 模板字符串导致 ${...} 抛出 ReferenceError、代码块反引号闭合语法错误及反斜杠转义崩溃的问题，在插件渲染层自动改写为安全容器（hidden textarea）并转义闭合标签，彻底解决白屏。
- 新增回执渲染模板配置字段 `receipt_template`（展示名「渲染模板」）：可自由指定用于渲染回执的 T2I 模板（如 `base`、`astrbot_vitepress`、`astrbot_powershell` 或其它自定义模板）；留空时自动沿用 AstrBot 当前全局选中的模板。
- 兼顾 remote 与 local 渲染策略及多层兜底保障：跟随 AstrBot 的 `t2i_strategy`；当网络渲染失败或检测到返回空白图片时自动尝试本地渲染兜底；若仍不可用则安全回退为纯文本发送，杜绝静默丢失内容。
- 优化 `_t2i_hard_breaks` 换行处理：避免对围栏代码块内部行追加尾随空格而破坏代码原样格式。

## 1.5.9 - 2026-09-22

- 配置页按「接入与模型 / 取回回执 / 图床上传 / 沙盒环境回收」分组。旧的平铺配置会在插件加载时迁入对应分组，Key 和图床地址不会因此被清空。
- 原来的「取回回执图片上传图床」拆成两个开关：`receipt.image_receipt`（图片回执，默认开）和 `image_host.enabled`（启用上传图床，沿用旧开关）。
- 图片回执只用于状态为 `completed` 的取回。进行中、失败或其它状态仍发文本。关闭图片回执后一律发文本。
- 关闭「启用上传图床」后，不再挂载上传 Token、不再要求沙盒上传文件，也不再上传回执图片或 md。

## 1.5.8 - 2026-09-22

- 取回回执改为渲染成图片直接发给用户，并附参考链接与产物链接，不再往聊天窗口倒长文本。
- `/agretrieve` 与 `/agcontinue` 的自动取回都走图片；渲染或发送失败时自动回退为原来的文本回执。
- 新增配置 `upload_render_image`（默认开）：是否把回执图片上传图床生成参考链接；关闭后只发本地渲染图片。
- 新增配置 `sandbox_agent`（默认 `antigravity-preview-05-2026`）：Interactions 请求的 agent 型号，Google 发布新沙盒版本后只改配置即可适配。误填 `gemini-*` 模型名时回退默认型号并在回执中给出警告。
- LLM 工具 `retrieve_sandbox_task` / `continue_sandbox_task` 取到事件后同样直接发图，只回简报，避免智能体再复述全文刷屏。
- 修复中文文件名丢失扩展名点号：`_safe_upload_name` 改为先拆主名和后缀再分别清理，`文转图模板.tar` 不再被剥成无扩展名的 `260922123022_tar`，而是 `260922123022_file.tar`（`archive.tar.gz` 这类双扩展名也完整保留，图床 content-type 也能正确识别）。

## 1.5.7 - 2026-09-21

- 修复 `/agcontinue` 因 `GreedyStr` 带默认值而被框架只吃第一个词，任务文本被截断。
- 不写类型的续接不再把「【文件上传要求】」塞进沙盒；取回后由插件上传新的 `*_result.md`。正文与上一轮哈希相同则不重传。
- 指定 html/png 等类型时仍由沙盒按类型上传。
- 默认 md 的聊天取回保留沙盒压缩过的回执，并附上图床链接；回执本身超过约 2000 字时截断。
- 续接若先自动取回上一轮，会把取回内容发到聊天窗口。

## 1.5.6 - 2026-09-21

- 提交 / 续接指令改为 `[类型...] <任务文本>`：类型写在任务文本前面，任务文本整段保留（含空格）。
- 帮助 `/aghelp` 与 README 同步新写法；顺手修正 README 里过时的取回 / 续接参数。

## 1.5.5 - 2026-09-18

- 环境回收指令改名为 `/agenvcleanup`。
- 支持 `/agenvcleanup <短号>` 立即删除指定沙盒，不再把短号当成非法参数打回。
- 帮助说明补清：不带参数只扫本插件记录过的闲置环境；`all` 扫整个项目可回收环境；两者仍受 TTL / 最近保留保护。

## 1.5.4 - 2026-09-18

- 运行时状态文件改存 `data/plugin_data/astrbot_plugin_antigravity_sandbox/`，不再写插件自身目录。
- 任务绑定只保存 Gemini Key 指纹，明文 Key 仅保留在插件配置中；启动时自动迁移并清洗旧文件。

## 1.5.3 - 2026-09-16

- 续接前若尚未取回，会先自动取回上一轮；`status` 不是 `completed` 则只返回当前状态、不提交续接。
- 提交 / 续接指令回执补充上述说明。

## 1.5.2 - 2026-09-16

- 短号改为四位（`0001` 起），续接不再另分配新号，直接覆盖同一短号。
- 续接始终接到该沙盒最新一轮 interaction，避免从祖先 id 分叉导致内容错乱。

## 1.5.1 - 2026-09-16

- 短号改为真正的五位序号（`00001` 起），不再占用末位做续接计数。
- 续接短号改为 `五位短号_` + 产物 URL 时间戳（`YYMMDDHHMMSS`）的后六位，例如 `00031_143052`。
- 取回 / 续接仍可用上一轮返回的短号；旧的五位短号（含末位续接位）可继续解析。
